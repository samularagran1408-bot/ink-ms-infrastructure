-- El esquema de suscripciones vive en 10-subscriptions-schema.sql.
-- Este archivo se conserva vacío a propósito: docker-entrypoint-initdb.d
-- ejecuta todos los .sql en orden y no debe crear las mismas tablas dos veces.
SELECT 1;
